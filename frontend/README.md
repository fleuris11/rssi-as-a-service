# Frontend

React 18 + Vite + Tailwind CSS. See the [repository README](../README.md) for how this fits into
the project and how to run it alongside the backend.

```bash
npm install
npm run dev      # http://localhost:5173
npm run lint
npm run build
```
