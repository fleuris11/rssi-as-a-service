# Copy to .env at the repo root. Used by docker-compose.yml to configure
# the postgres service; keep POSTGRES_PASSWORD in sync with the
# DATABASE_URL password in backend/.env.

POSTGRES_DB=rssiasservice
POSTGRES_USER=rssiasservice
POSTGRES_PASSWORD=change-me
